// Demo script để simulate extension behavior
console.log('🛡️ Phishing Detection Extension Test Page loaded');
console.log('Current URL:', window.location.href);

// Simulate popup behavior
document.addEventListener('DOMContentLoaded', () => {
    console.log('Page loaded, extension should show status for:', window.location.href);
});